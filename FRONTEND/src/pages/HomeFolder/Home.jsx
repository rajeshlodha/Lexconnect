import { LandingPageContent } from "../../compnents/Nav";

export const Home = () => {
  // This component's only job is to render the content for the landing page.
  // The layout (header/footer) is handled by the router.
  return <LandingPageContent />;
};
